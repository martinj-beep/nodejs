import express from "express";

const router = express.Router();

router.get("/atendees", );
router.post("/atendees", );
router.put("/atendees/:id", );
router.delete ("/atendees/:id", )

export default router